    <div class="container">
      <div class="profile text-center">
        <div class="main">
          <div class="main-content">
            <img src="images/seb.jpeg" alt="" width="150" class="img-circle">
        <p>Marque: <%= @product.marque %></p>
        <p>Adresse: <%= @product.address %></p>
        <p>Matière: <%= @product.matiere %></p>

        <%= link_to 'Acheter', products_path, class:"btn btn-primary" %>
          </div>
        </div>
        <div class="stat">
          <div class="stat-content">
            <h3>3,536</h3>
            <p>contributions</p>
          </div>
        </div>
        <div class="stat">
          <div class="stat-content">
            <p>Taille: <%= @product.taille %></p>
            <p>Etat: <%= @product.etat %></p>
            <p>Couleur: <%= @product.couleur %></p>
          </div>
          </div>
        <div class="stat">
          <div class="stat-content">
            <p>prix</p>
            <h3><strong><%= @product.price %>euros </strong></h3>
          </div>
          </div>
        <div class="description">
          <div class="description-content">
            <p>Description: </p>
            <p class="text-justify">
              <p><%= @product.description %></p>
              <%= link_to 'Editer', edit_product_path(@product), class:"btn btn-primary" %>
              <%= link_to 'Retour', products_path, class:"btn btn-primary" %>
            </p>
          </div>
        </div>
      </div>
    </div>

    <!-- code pour afficher toutes les photos
          <div class="photo1">
        <% @product.photos.each do |photo| %>
        <%= cl_image_tag photo.path, width: 100, height: 100, crop: :fill %>
        <% end %>
      </div>
--!>
